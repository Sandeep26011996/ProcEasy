{
  "scripts": {
    "test" : " npx cypress run --headed",
    "cy:parallel" : " cypress-parallel -d login.cy.js -t 2 -s test",
    "cypress:chrome":"cypress run --browser chrome --spec cypress/itegration/pom/*.spec.js"
  },
  
  "devDependencies": {
    "cypress": "^12.17.4",
    "cypress-mochawesome-reporter": "^3.5.1",
    "cypress-slow-down": "^1.2.1",
    "cypress-xpath": "^2.0.1"
  },
  "dependencies": {
    "cypress-file-upload": "^5.0.8",
    "cypress-parallel": "^0.13.0",
    "moment": "^2.29.4"
  }
}
